


import PlaybackVisualization from 'visualization';

class ASPGraphVisualizer extends PlaybackVisualization {
  // data given is the full solving data to play back, clasp/clingo JSON
  setPlaybackData(data) {};

  // starts the visualization (playback)
  start() {};
}

randomSeed
hierarchical
- enabled
- levelSeparation
- direction
- sortMethod

layoutAttr(randomSeed, Number).
layoutAttr(hierarchical, enabled(true)).

layoutAttr(hierarchical, enabled, true).
layoutAttr(hierarchical, direction, "UD").

layoutAttr(hierarchical, (enabled(true), direction("UD"), ))




xAttr(foo, bar, Value)
->
xMapped.foo.bar = Value;

xAttr(foo, values((bar, Value), (baz, Value2))).
->
xMapped.foo = {bar: Value, baz: Value2}


foo(bar(1), 1, bar((("asd"), 0)), 2).

{ident: foo,
 value: [
   {ident: bar,
    value: 1},
   1,
   {ident: bar,
    value: [
      ["asd"],
      0
    ]},
    2
 ]}


{foo: [
  {bar: [1]},
  1,
  {bar: [
    ["asd"],
    0
  ]},
  2
]}






